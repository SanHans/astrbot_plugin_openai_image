{
  "base_url": {
    "type": "string",
    "description": "OpenAI 兼容 API 地址",
    "default": "https://api.openai.com/v1",
    "hint": "填写兼容 OpenAI Images API 的根地址，建议精确到 /v1，例如 https://api.openai.com/v1。"
  },
  "api_key": {
    "type": "string",
    "description": "API Key",
    "default": "",
    "hint": "留空时会尝试读取环境变量 OPENAI_API_KEY。",
    "obvious_hint": true
  },
  "model": {
    "type": "string",
    "description": "图像模型",
    "default": "gpt-image-2",
    "hint": "默认使用 OpenAI 官方的 gpt-image-2，也可以改成兼容服务上的其他图像模型名。"
  },
  "timeout": {
    "type": "int",
    "description": "统一超时兜底（秒）",
    "default": 180,
    "min": 30,
    "max": 600,
    "hint": "兼容旧配置。若未单独配置 generation_timeout / download_timeout，则分别回退到这里或其衍生值。"
  },
  "generation_timeout": {
    "type": "int",
    "description": "生图请求超时时间（秒）",
    "default": 180,
    "min": 30,
    "max": 600,
    "hint": "控制调用 images/generations 接口的总超时时间。"
  },
  "download_timeout": {
    "type": "int",
    "description": "图片下载超时时间（秒）",
    "default": 60,
    "min": 5,
    "max": 300,
    "hint": "当上游返回图片 URL 而不是 base64 时，控制下载图片文件的总超时时间。"
  },
  "retry_count": {
    "type": "int",
    "description": "失败重试次数",
    "default": 1,
    "min": 0,
    "max": 5,
    "hint": "对 408/429/500/502/503/504、网络异常和本地超时进行重试。值为 1 表示失败后额外再试 1 次。"
  },
  "retry_backoff_seconds": {
    "type": "int",
    "description": "重试退避秒数",
    "default": 2,
    "min": 0,
    "max": 30,
    "hint": "每次重试前按 attempt × retry_backoff_seconds 等待，例如默认依次等待 2 秒、4 秒。"
  },
  "proxy_url": {
    "type": "string",
    "description": "代理地址",
    "default": "",
    "hint": "可选。填写 HTTP/HTTPS 代理地址，例如 http://127.0.0.1:7890 。会同时用于生图请求和远程图片下载。"
  },
  "max_concurrent_tasks": {
    "type": "int",
    "description": "最大并发任务数",
    "default": 3,
    "min": 1,
    "max": 10,
    "hint": "限制同时发起的绘图请求数量，避免并发过高导致上游接口限流。"
  },
  "default_prompt_prefix": {
    "type": "text",
    "description": "默认提示词前缀",
    "default": "",
    "hint": "会自动拼接到每次绘图请求前，可用于统一风格、画风或安全限制。"
  },
  "detailed_logging": {
    "type": "bool",
    "description": "启用详细日志",
    "default": true,
    "hint": "开启后会记录平台类型、润色前后提示词、请求摘要、生成耗时、图片发送方式等详细信息。"
  },
  "size": {
    "type": "string",
    "description": "图片尺寸",
    "default": "auto",
    "hint": "支持 auto 或合法分辨率，如 1024x1024、1536x1024、1024x1536、2048x2048。"
  },
  "quality": {
    "type": "string",
    "description": "图片质量",
    "default": "auto",
    "options": [
      "auto",
      "low",
      "medium",
      "high"
    ]
  },
  "background": {
    "type": "string",
    "description": "背景模式",
    "default": "auto",
    "options": [
      "auto",
      "opaque",
      "transparent"
    ],
    "hint": "gpt-image-2 官方目前不支持 transparent；如果你的兼容服务支持，可自行切换。"
  },
  "output_format": {
    "type": "string",
    "description": "输出格式",
    "default": "png",
    "options": [
      "png",
      "jpeg",
      "webp"
    ]
  },
  "output_compression": {
    "type": "int",
    "description": "输出压缩率",
    "default": 90,
    "min": 0,
    "max": 100,
    "hint": "仅在输出格式为 jpeg 或 webp 时生效。"
  },
  "moderation": {
    "type": "string",
    "description": "内容过滤强度",
    "default": "auto",
    "options": [
      "auto",
      "low"
    ]
  },
  "image_count": {
    "type": "int",
    "description": "单次生成图片数量",
    "default": 1,
    "min": 1,
    "max": 4
  },
  "send_prompt_back": {
    "type": "bool",
    "description": "返回修订后的提示词",
    "default": false,
    "hint": "仅当上游接口返回 revised_prompt 时才会发送。"
  }
}
